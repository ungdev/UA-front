/// This component is used to pass parameters for each element to the ScrollingParallax component
export function ScrollingParallaxElement({ speed, children }: { speed: number; children: React.ReactNode}) {
  return children;
}
