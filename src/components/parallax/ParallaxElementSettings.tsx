'use client';

/// This component is used to pass parameters for each element to the Parallax component
export default function ParallaxElementSettings({ speed, children, className }: { speed: number; children: React.ReactNode; className: string }) {
  return children;
}
